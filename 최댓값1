#include<stdio.h>
int main(void)
{
    //3개의 정수를 입력해서 가장 큰 수 구하는 방법1

    int a,b,c,x;
    printf("3개의 정수를 입력\n");
    scanf_s("%d %d %d",&a ,&b, &c);
    if(a>b){
        if(a>c) {x=a;}
        else {x=b;}
    }
    else{
        if(b>c) {x=b;}
        else {x=c;}
    }

    return 0;
}
