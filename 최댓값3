#include<stdio.h>
int main(void)
{
	//3개의 정수를 입력해서 최댓값 출력3
	int a, b, c, x;
	printf("3개의 정수 입력\n");
	scanf_s("%d %d %d", &a, &b, &c);
  x = a;
	if (x < b) { x = b; }
	if (x < c) { x = c; }
	printf("가장 큰수는=%d\n", x);

	return 0;
}
